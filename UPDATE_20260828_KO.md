# SSHS Ambassadors 2026-08-28 소규모 업데이트 적용 안내

이 업데이트는 기존 Git 저장소를 새로 만들지 않습니다. 현재 사용 중인
`sshs-ambassadors-v28-recovered` 폴더에 수정 파일만 덮어쓴 뒤, 새 SQL 하나를
실행하고 GitHub에 올리면 됩니다.

## 포함된 변경

- 긴 투어 설명의 글자 크기를 줄이고, 내용이 아주 길면 설명 영역 안에서 스크롤
- 로그아웃 방문자도 게시된 설명과 사진 열람 가능
- 로그인한 member/admin이 설명과 연결 사진을 한 번에 삭제 가능
- member가 새로 등록한 설명도 즉시 공개
- `Science Fair Open Tour` 설문 및 연결 데이터 삭제

## 1. 수정 파일 덮어쓰기

압축을 푼 업데이트 폴더 안의 파일을 현재 Git 저장소에 복사하고 같은 이름의
파일을 덮어씁니다. `.git`과 `.env.local`은 삭제하거나 바꾸지 않습니다.

## 2. 코드 검사

VS Code에서 현재 Git 저장소를 열고 PowerShell에서 실행합니다.

```powershell
npx.cmd eslint . --ignore-pattern dist --ignore-pattern dist-pages --ignore-pattern .next
```

`error`가 0개인지 확인한 뒤 실행합니다.

```powershell
npm.cmd run build:pages
```

마지막에 `built in ...`이 나오면 정상입니다.

## 3. Supabase SQL 적용

Supabase Dashboard에서 사이트가 사용하는 프로젝트를 열고 `SQL Editor → New query`로
이동합니다. 아래 파일 전체를 복사해 한 번 실행합니다.

```text
supabase/migrations/202608280001_public_notes_delete_and_survey_cleanup.sql
```

정상 결과는 `Success. No rows returned`입니다. 이 SQL은 기존 비공개 설명을 공개로
전환하며, `Science Fair Open Tour`와 그 설문에 연결된 질문·응답을 삭제합니다.

## 4. GitHub에 올리기

```powershell
git status --short
```

`.env.local`과 `supabase/.temp/`가 표시되지 않는지 확인합니다. 이어서 이번 업데이트
파일만 추가합니다.

```powershell
git add -- app/AmbassadorsApp.tsx app/globals.css app/portalData.ts SOURCE_VERSION.txt UPDATE_20260828_KO.md supabase/migrations/202608280001_public_notes_delete_and_survey_cleanup.sql
```

```powershell
git commit -m "Make tour notes public and add signed-in deletion"
```

```powershell
git push origin main
```

GitHub의 `Actions`에서 최신 `Deploy to GitHub Pages`가 녹색 체크가 된 뒤 배포 사이트에서
`Ctrl+F5`를 누릅니다.

## 5. 기능 확인

1. 로그아웃 상태로 Tour에 들어가 `사진·설명 보기`가 나타나는지 확인합니다.
2. 로그인하면 사진 화면과 설명 화면에 `사진·설명 삭제`가 나타나는지 확인합니다.
3. 설명을 하나 등록하고 로그아웃한 뒤에도 보이는지 확인합니다.
4. Survey 목록에 `Science Fair Open Tour`가 사라졌는지 확인합니다.

삭제 버튼은 하나의 설명과 그 설명에 연결된 모든 사진을 함께 삭제합니다. 삭제된
내용은 사이트에서 복구할 수 없으므로 확인 창에서 한 번 더 동의해야 실행됩니다.
